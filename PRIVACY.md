# 观墨（GuanMo）隐私政策

**更新时间：2026 年 7 月 28 日**

## 1. 概述

观墨是一款本地优先的 Markdown 阅读、编辑与 AI 辅助应用。本隐私政策说明观墨如何访问、存储和处理用户数据，以及用户使用第三方 AI、Embedding 和联网搜索服务时的数据流向。

观墨无需注册账户。当前版本不包含广告、用户行为分析或遥测功能，开发者不运营用于接收用户文档、知识库或 AI 请求内容的服务器。

## 2. 数据收集

观墨不会收集、上传或保存用户的 Markdown 文档内容至开发者运营的服务器，也不会向开发者发送聊天记录、知识库内容、长期记忆、API Key 或本地文件路径。

为提供应用功能，以下数据可能保存在用户自己的设备上：

- 用户创建、打开或编辑的 Markdown 文件；
- 最近文件、收藏、工作区、标签页和文件访问授权等本地状态；
- AI 会话消息、长期记忆和相关应用设置；
- 本地知识库的文档元数据、语义分块、索引状态和向量 Embedding；
- 用户自行配置的 AI、Embedding 和联网搜索服务信息及 API Key。

这些本地数据不会由开发者远程访问。

## 3. 本地文件与文件系统访问

观墨仅为用户明确选择或授权的文件、文件夹和工作区访问本地文件系统。相关权限用于：

- 打开、读取、编辑、保存和管理 Markdown 文件；
- 展示工作区文件树，并恢复最近文件、收藏和标签页；
- 读取或写入用户插入到 Markdown 文档中的本地图片等资源；
- 为用户主动加入知识库的 Markdown 文件建立和更新本地索引；
- 按用户操作导出文件或应用数据备份。

桌面版仅将 `.md` 文件作为编辑器文档打开。文件和工作区授权记录保存在本机应用配置目录，用于在后续启动时恢复仍然有效的访问范围。观墨不会借此扫描或上传设备中的其他文件。

## 4. 数据存储

用户的 Markdown 文件默认保存在用户选择的本地位置。观墨不会把这些文件复制到开发者服务器。

桌面版的应用数据主要保存在本机应用配置目录中的 SQLite 数据库。数据可能包括 AI 会话、长期记忆，以及本地知识库使用的文档元数据、文本分块和向量 Embedding。知识库检索和索引数据默认仅在本机保存；删除知识库索引不会自动删除用户原始 Markdown 文件。

API Key 由用户自行提供。在 Windows 上，观墨使用 Windows 数据保护 API（DPAPI）加密后将其保存在本机，不把 API Key 写入普通设置或常规数据备份。

用户主动导出的备份文件由用户自行选择保存位置。备份可能包含会话、消息和长期记忆，但不包含 API Key；用户应自行妥善保管备份文件。

## 5. AI 与 Embedding 服务

观墨不内置开发者持有的 AI API Key。使用 AI 或远程 Embedding 功能前，用户需要自行选择模型服务商或自定义兼容服务，并配置相应 API Key；用户也可以选择 Ollama 等本地服务。

当用户主动使用 AI 功能时，观墨会将完成该请求所必需的数据直接发送到用户选择的服务端点。这些数据可能包括：

- 用户输入的提示词或问题；
- 用户明确提供的选区、文件、文件夹或知识库检索片段；
- 为保持对话所需的相关会话上下文；
- 用户启用并允许用于当前请求的长期记忆或工具结果。

当用户使用远程 Embedding 服务建立或查询知识库时，生成向量所需的文本分块或查询文本会发送给该服务商。如果用户使用本地模型或本地 Embedding 服务，这些请求可在本机处理。

AI 和 Embedding 请求由用户选择的服务商依其隐私政策处理。开发者不会接收或保存这些请求内容。用户应在使用前查阅所选服务商的隐私政策、数据保留规则和使用条款。

## 6. 联网搜索与更新检查

联网搜索仅在用户启用该功能并主动发起搜索、AI 工具调用或连接测试时触发。届时，搜索关键词会发送给用户选择的搜索服务，例如 DuckDuckGo、Tavily、Serper、Brave Search 或用户配置的自定义服务。搜索服务可能按照其自身政策处理查询内容、IP 地址和必要的网络请求信息；开发者不会保存搜索关键词或搜索结果。

桌面版会通过 GitHub Releases 检查观墨的新版本。该请求只用于获取版本和发布说明，不包含用户文档、聊天记录或知识库内容。GitHub 可能按照其自身隐私政策处理 IP 地址、请求时间和请求头等必要的网络信息。

## 7. 第三方服务

观墨可能连接以下由用户选择或功能所需的第三方服务：

- AI 模型和 Embedding 服务商；
- 联网搜索服务商；
- GitHub Releases 更新信息服务；
- 用户自行配置的自定义 API 或本地模型服务。

第三方服务独立决定其数据处理方式，观墨开发者无法控制其日志、保留期限或跨境传输安排。用户可以不配置或不使用 AI、远程 Embedding 和联网搜索功能；Markdown 的本地阅读与编辑功能不依赖这些服务。

## 8. 数据安全

观墨采用受限的本地文件授权机制，仅访问用户明确选择或授权的路径。外部网络请求通过桌面端受限请求代理执行，并对目标地址、协议和访问范围进行校验。自定义或本地服务端点可能需要用户确认授权。

任何软件或网络传输都无法保证绝对安全。用户应妥善保管 API Key、原始文档和导出的备份，并根据所选第三方服务的安全要求管理账户与凭据。

## 9. 用户权利与控制

用户始终保有对本地文档和应用数据的控制权，可以：

- 决定打开、保存和加入知识库的文件或工作区；
- 查看、编辑或删除本地 Markdown 文件；
- 管理知识库索引、AI 会话和长期记忆；
- 修改或移除 API Key、模型端点和联网搜索配置；
- 撤销已保存的自定义网络端点授权；
- 导出或导入普通应用数据备份；
- 停止使用第三方 AI、Embedding 或联网搜索服务。

由于开发者不持有用户的本地数据，开发者无法代用户访问、恢复或删除这些数据。若要彻底移除本地应用数据，用户可使用应用内相应管理功能，并按照操作系统方式删除观墨的本地应用数据。

## 10. 隐私政策更新

如果观墨的功能或数据处理方式发生变化，本政策可能相应更新。更新后的版本将发布在本仓库，并在文档顶部注明新的更新时间。

## 11. 联系方式

如对本隐私政策或观墨的数据处理方式有疑问，请通过项目的 [GitHub Issues](https://github.com/we-used-to-be/Guanmo-open/issues) 联系项目维护者。提交问题时，请勿公开粘贴 API Key、私人文档内容或其他敏感信息。
