# UI 组件契约

涉及按钮新增、修改或视觉层级调整时，修改前必须读取本文件。

软件自有弹窗的结构、主题令牌和交互约束见 [`modal.md`](./modal.md)，以“设置 → 快捷操作 → 新增操作”弹窗为模板。

- 按钮默认使用 `type="default"`。
- `type="primary"` 仅用于提交表单、确认关键操作等明确的主要操作。
- `type="text"` 用于文字链接样式，`type="danger"` 用于删除等危险操作。
- 紧凑场景使用 `size="small"`；不指定 `size` 时使用默认中等尺寸。
